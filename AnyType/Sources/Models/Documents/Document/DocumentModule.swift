import Foundation

enum DocumentModule {
    /// We have `Container` with all data.
    /// We have `Document` with EventHandler and PageDetailsViewModel ( or model? )
    ///
    
    enum Document {}
}
