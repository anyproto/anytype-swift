import Foundation
import SwiftUI

extension Color {
    // Color is stored in the assets of the main target. Then it is available in LaunchScreen.storyboard
    static let launchColorsCircle = Color("LaunchColors/circle")
}
