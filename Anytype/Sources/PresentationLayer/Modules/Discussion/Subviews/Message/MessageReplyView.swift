import Foundation
import SwiftUI

struct MessageReplyView: View {
    
    let author: String
    let description: String
    let icon: Icon
    let isYour: Bool
    
    var body: some View {
        HStack(spacing: 0) {
        
        }
    }
}
