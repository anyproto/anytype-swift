import SwiftUI

final class MessageAttachmentsGridLayoutBuilder {
    func makeGridRows(countItems: Int) -> [Int] {
        if countItems == 0 {
            return []
        }
        
        if countItems == 1 {
            return [1]
        }
        
        if
    }
}
